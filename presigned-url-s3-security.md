---
title: "Presigned URLで画像アップロードを設計したら セキュリティの多層防御が見えてきた"
emoji: "✨"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: [AWS]
published: false
---
1.なぜPresigned URLを選んだのか（設計判断）

画像アップロードの実装を考えるとき、最初に思い浮かぶのは「フロントエンドからバックエンドに画像を送り、バックエンドがS3に転送する」というプロキシ型の構成だ。
しかしこの方式には構造的な無駄がある。バックエンドが画像のバイナリを中継するため、同じデータがブラウザ→バックエンド→S3と2回転送される。同時アップロードが増えるほどバックエンドのCPUと帯域（ネットワークの転送容量）を消費し、バックエンド自体が処理のボトルネックになる。
そこで採用したのがPresigned URL方式だ。バックエンドがS3への署名付きURLを発行し、ブラウザはそのURLを使ってS3に直接アップロードする。バックエンドが扱うのはメタデータ・署名付きURL・S3キーのみで、画像のバイナリ本体には一切触れない。帯域もCPUも節約でき、同時アップロードが増えてもバックエンドの負荷はほぼ変わらない。

2.三者の役割分担

Presigned URL方式では、ブラウザ・App Server・S3の3者がそれぞれ明確な役割を持つ。
ブラウザはユーザー操作の窓口だ。ファイル選択やドラッグ&ドロップを受け付け、App ServerへのHTTPリクエスト（POST・PATCH・GET・DELETE）と、S3へのバイナリの直接送信を担う。アップロードの進捗をプログレスバーで表示したり、完了・エラーをユーザーに通知したりするのもブラウザの責務だ。
App Serverはコントロールタワーの役割を担う。JWTによる認証・認可、Presigned URLの発行、DBへのメタデータの永続化を行う。画像のバイナリ本体には一切触れず、「誰が・何を・どこに置くか」の管理だけを担当する。
S3はバイナリデータの保管庫だ。ブラウザから直接PUTされたファイルを受け取り、URLに埋め込まれた署名をIAMの秘密鍵で再計算・照合することで、App Serverが正規に発行したリクエストかどうかを検証する。

![三者の役割分担](/images/three_party_roles-v3.png)

3.秘密鍵による処理

Presigned URLの中核にあるのはHMAC-SHA256という署名アルゴリズムだ。App ServerはPresignedURLを生成するとき、IAMの秘密鍵を使って「HTTPメソッド・対象のS3キー・有効期限・発行日時」を材料にHMAC-SHA256でハッシュ値を計算する。この値が署名本体であり、URLのクエリパラメータとして材料とともに埋め込まれる。

~~~＜Presigned URLの一例＞
https://s3.amazonaws.com/app-images-prod/uploads/user-1/abc.jpg
  ?X-Amz-Algorithm=AWS4-HMAC-SHA256
  &X-Amz-Expires=300
  &X-Amz-Signature=a3f9b2c1...  ← 署名本体
~~~

ブラウザはこのURLをそのままS3へのPUTリクエストに使う。S3はリクエストを受け取ると、URLから材料を取り出し、同じIAM秘密鍵で署名を再計算する。再計算した値とURLに含まれる署名が一致すれば「App Serverが正規に発行したリクエスト」だとしてアクセスを許可する。この仕組みの重要な点は、秘密鍵がApp ServerとAWSの間だけに存在することであり、ブラウザは秘密鍵を持たないことからURLを改ざんしても正しい署名を作れない。対象キーを一文字でも書き換えれば再計算した署名が食い違い、S3に即座に拒否されるようになっているのだ。

4.セキュリティの多層防御
ファイルアップロードのセキュリティは一か所で担保できるものではない。今回の設計では5層の防御を重ねている。
第１層はフロントエンドのバリデーションだ。Content-Typeとファイルサイズをブラウザ上でチェックし、明らかにおかしいリクエストをUIの段階で弾くことができる。ただし、ブラウザの検証は開発者ツールで簡単に改ざんできるため、これだけでは不十分だ。
第２層はApp Serverでのバリデーションだ。JWTのuser_idとDBのuser_idを照合して「誰がどのリソースを操作できるか」をビジネスロジックで判定する。同時にContent-Type・拡張子・サイズをサーバー側で再検証する。フロントエンドを迂回した直接リクエストもここで弾くことができる。
ここで重要な限界として、第1層から第3層はいずれも「ブラウザが申告したContent-Typeの文字列」を検証対象としてみている。見ている乗法の性質は同じで、悪意あるユーザーがContent-Typeを偽装してimage/pngと申告しながら実行ファイルを送った場合、この3層はまとめてすり抜けられる結果となる。
第4層はIAMロールによるAWSインフラレベルの認可処理となる。「このAWSサービスはS3を操作してよいか」をAWSが判定する。App Serverを経由しない不正なアクセスをインフラの層で遮断する。
第5層はLambdaによるマジックバイト検証だ。S3に保存されたファイルのバイナリ本体を直接読み、先頭数バイトのマジックバイトでファイルの実形式を確認する。JPEGなら必ずFF DB FFで始まるという性質を利用して、Content-Typeを偽装したファイルを検出する。sんこくされた文字列を一切信用せず、ファイルの実態だけで判定するこの層が全ての層をすり抜けた偽装の最終的な砦となる。
各層は独立して動作し、前の層をすり抜けたものを次の層が捕まえる構造になっている。どれか一層が突破されてもほかの層が機能するのが多層防御の本質だ。

5.気づいたこと
設計を進める中で、重要な見落としに気づいた。アップロード（PUT）についてはPresigned URL方式を採用して設計していたが、ダウンロード(GET)の公開範囲について明確に定義していなかったのだ。
最初の設計ではGETもバックエンドのプロキシ型を採用していた。しかし「画像はだれでも見られるのか、本人だけなのか」という前提が要件に明記されていなければ、アーキテクチャの選択自体が変わってくる。誰でも見られる場合はCloudFront経由で配信すればいいが、本人だけに限定する場合はGETの度にApp ServerでJWT検証を行い、GET用のPresigned URLを発行する設計が必要になる。
画像アップロードという一見シンプルな機能でも「誰が・いつ・どのリソースを・どのように扱えるか」を要件の段階で明確にしなければ、設計の手戻りが発生する。
この教訓は医療・金融といったセキュリティ要件の厳しいサービスでは特に重要だ。一般的なWebアプリではCDNによる公開配信で十分な場面でも、機密性の高いデータを扱う場合はVPC内のプライベートネットワーク・Presigned URLによるアクセス制御・Lambdaでの実体検証を組み合わせた設計が求められる。どのレベルのセキュリティが必要かは、実装を始める前に確定させるべき最重要事項の一つだ。

参考資料 AWS公式ドキュメント
　　　
